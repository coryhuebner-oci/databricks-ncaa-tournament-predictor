# This is a Databricks asset bundle definition for databricks-ncaa-tournament-predictor.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: databricks-ncaa-tournament-predictor

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-7be6322b-fcce.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-7be6322b-fcce.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/huebnerc@objectcomputing.com to make sure we only have a single copy.
      root_path: /Workspace/Users/huebnerc@objectcomputing.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: huebnerc@objectcomputing.com
        level: CAN_MANAGE
